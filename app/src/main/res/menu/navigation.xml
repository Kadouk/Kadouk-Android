<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/account"
        android:title="@string/tab_account"
        android:icon="@mipmap/ic_tab_profile"/>

    <item android:id="@+id/download"
        android:title="@string/tab_download"
        android:icon="@mipmap/ic_tab_updates" />

    <item android:id="@+id/search"
        android:title="@string/tab_search"
        android:icon="@mipmap/ic_tab_search" />

    <item android:id="@+id/game"
        android:title="@string/tab_game"
        android:icon="@mipmap/ic_tab_game"/>

</menu>